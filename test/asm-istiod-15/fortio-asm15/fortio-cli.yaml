---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: cli-fortio15
  namespace: fortio-asm15
spec:
  replicas: 1
  template:
    metadata:
      annotations:
        sidecar.istio.io/proxyCPU: "500m"
        # Readiness broken currently
        #status.sidecar.istio.io/port: "0"
      labels:
        app: cli-fortio
        version: v1
    spec:
      containers:
      - name: echosrv
        image: costinm/fortio:latest
        imagePullPolicy: Always
        args:
          - load
          - -allow-initial-errors
          - -t
          - "0"
          - -c
          - "1"
          - -qps
          - "1"
          - http://fortio-asm15.fortio-asm15:8080/echo?size=5000
        resources:
          requests:
            cpu: 100m
            memory: "100M"
          limits:
            cpu: 800m
            memory: "1G"
