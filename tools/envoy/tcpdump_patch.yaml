spec:
  template:
    spec:
      containers:
        - name: tcpdumper
          # corfr/tcpdump
          image: docker.io/dockersec/tcpdump
#          command:
#            - /bin/sleep
#            - infinity
