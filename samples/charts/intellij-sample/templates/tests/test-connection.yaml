apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "intellij-sample.fullname" . }}-test-connection"
  labels:
    {{- include "intellij-sample.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "intellij-sample.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
