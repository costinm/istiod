apiVersion: v1
kind: Service
metadata:
  name: {{ include "intellij-sample.fullname" . }}
  labels:
    {{- include "intellij-sample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "intellij-sample.selectorLabels" . | nindent 4 }}
